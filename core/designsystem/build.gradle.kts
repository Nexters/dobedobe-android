plugins {
    alias(libs.plugins.dobedobe.android.compose)
    alias(libs.plugins.dobedobe.android.library)
}

android {
    namespace = "com.chipichpi.dobedobe.core.designsystem"
}

dependencies {
}
