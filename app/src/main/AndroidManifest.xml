<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DobeDobeApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DobeDobe.Starting"
        tools:targetApi="31">
        <!-- adjustResize ensures that the main window resizes to make room for the soft keyboard-->
        <!--        TODO 참고 자료 정리해서 PR 에 적기-->
        <!--        jetNews: https://github.com/android/compose-samples/blob/main/JetNews/app/src/main/AndroidManifest.xml-->
        <!--        공식문서: https://developer.android.com/develop/ui/compose/layouts/insets#insets-setup-->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>